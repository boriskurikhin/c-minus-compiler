The Semantic analyzer tree is:
Entering the global scope:
    Entering the function scope for function size:
        multiplier: INT
    Leaving the function scope
    Entering the function scope for function main:
        Entering a new while block:
            inc: INT
        Leaving the while block
        Entering a new while block:
        Leaving the while block
        Entering a new if block:
            Entering a new if block:
                yy: INT
            Leaving the if block
            var: INT
        Leaving the if block
        array: INT[100]
        i: INT
    Leaving the function scope
    output: (INT) -> VOID
    input: () -> INT
    final_array: INT[100]
    size: (INT) -> INT
    main: () -> INT
Leaving the global scope
